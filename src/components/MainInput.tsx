import { useState } from "react";
import {Box, TextField, Stack} from "@mui/material";

export const MainInput = () => {
    return (
        <Stack>
            <TextField/>
        </Stack>
    )
}

